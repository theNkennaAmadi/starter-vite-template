// Import App class
import App from './App';

// Initialize the app once the DOM is fully loaded
window.addEventListener('load', () => {
//console.log('loaded');
});

// Create new App instance
const app = new App();

// Initialize the app
app.init();
